# ============================================================
# AI短剧制作自动流程 Agent 工作流  ——  依赖清单
# 安装: pip install -r requirements.txt
# ============================================================

# --- LLM 调用 -------------------------------------------------
openai>=1.30.0            # DeepSeek / OpenAI 兼容 API 客户端

# --- HTTP & 网络 ---------------------------------------------
requests>=2.31.0           # HTTP 请求（API 调用、资源下载）

# --- 数据校验 -------------------------------------------------
pydantic>=2.5.0            # 结构化数据模型 & 配置校验

# --- 图像处理 -------------------------------------------------
Pillow>=10.0.0             # 图片裁剪、缩放、合成字幕帧

# --- 视频合成 -------------------------------------------------
moviepy>=2.0.0             # 视频剪辑：图片序列→视频、配音叠加、字幕渲染

# --- 环境变量 -------------------------------------------------
python-dotenv>=1.0.0       # .env 文件加载（可选，config.py 已内置简易加载）

# --- TTS（可选） -----------------------------------------------
edge-tts>=6.1.0            # Microsoft Edge TTS（免费无 Key，_tts_edge_tts 后端需要）
